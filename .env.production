# Used by `vite build` only; .env still drives `npm run dev`.
# Without this, the committed .env bakes localhost:4000 into the production
# bundle and every visitor's browser calls their own machine.
VITE_BACKEND_URL=https://starfish-app-qp7nw.ondigitalocean.app/api/v1
